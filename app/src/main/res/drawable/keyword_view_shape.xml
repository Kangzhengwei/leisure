<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 实心 -->
    <solid android:color="@color/light_gray" />
    <!-- 边框 -->
    <stroke
        android:width="0dp"
        android:color="#5fc9af" />
    <!-- 圆角 -->
    <corners android:radius="50dp" />
    <!-- 边距 -->
    <padding
        android:bottom="8dp"
        android:left="16dp"
        android:right="16dp"
        android:top="8dp" />

</shape>